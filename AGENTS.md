# 코드 작성 규칙

- 이 프로젝트의 모든 텍스트 파일은 UTF-8 인코딩을 사용한다.
- 파일을 읽거나 수정할 때 UTF-8 인코딩을 명시적으로 사용한다.
- 기존 파일의 인코딩과 줄바꿈 형식(LF/CRLF)을 임의로 변경하지 않는다.
- 전체 파일을 다시 생성하거나 덮어쓰지 말고, 필요한 부분만 최소 범위로 수정한다.
- 한글 문자열이 깨지거나 "�" 문자가 발견되면 해당 파일을 수정하지 않는다.
- 인코딩이 불확실한 경우 먼저 파일 인코딩을 확인한 뒤 작업한다.
- 버튼 크기는 기준을 두어 인접한 버튼끼리는 크기를 맞춘다.
- 코드 작성 시 줄바꿈은 태그 단위로 한다.